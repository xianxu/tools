---
id: 000002
status: codecomplete
deps: []
github_issue:
created: 2026-08-20
updated: 2026-08-20
estimate_hours: 0.81
actual_hours: 1.05
---

# define REPL: bare invocation reads words, defines and speaks them

## Problem

`define <word>` is one lookup per shell invocation. Looking up five words means
five invocations, and re-hearing a word means retyping it. The pronunciation is
the part most worth repeating, and it is the most awkward to repeat.

## Spec

Bare `define` (no arguments) opens a read-eval loop:

```
$ define
› sycophantic
sycophantic  syc·o·phan·tic
/ˌsikəˈfan(t)ik/
  adjective  behaving or done in an obsequious way…
  ♫ playing 3×
›                      ← bare return: replays the SAME word 3×
› ephemeral
…
› ^C                   ← quits
```

- A word replaces the current one; a bare return replays the current one's
  **pronunciation only**, and writes **nothing to stdout at all** — no definition,
  no "♫ playing" line (operator, 2026-08-20). The screen stays as it was, so what
  you are hearing sits next to what you are reading. The repeat count is
  `-times N`, already customizable.

  The announcement therefore belongs to `defineOnce`, not to `speak`; `speak` is
  silent and the caller decides whether to say anything.
- Ctrl-C exits cleanly (and must not kill playback mid-file leaving a temp file).
- Not a mode flag: bare invocation is the trigger, so `define` alone is the
  friendly entry point and `define <word>` stays the one-shot path.
- The audio is **cached per word for the session**, so a replay does not re-fetch.
- Reuses `run()`'s existing seams unchanged — this is a loop around the same
  shell, not a second implementation (ARCH-DRY).
- The loop reads stdin unconditionally and prompts only when stdin is a terminal,
  so there is no second code path for batch use.
- **`echo word | define` does not work today** — verified: no-args exits 2 with
  usage. Making it define the word is a *new* capability of this issue, not
  behaviour being preserved. An earlier draft of this Spec claimed otherwise.

## Done when

- [x] Bare `define` on a TTY opens the loop; a word defines and plays it 3×.
- [x] Bare return replays the current word 3× with **no** second CDN request
      and **no** reprinted definition.
- [x] Ctrl-C exits 0, leaving no temp files behind.
- [x] `echo sycophantic | define` defines the word (new; it exits 2 today).
- [x] `go test -race ./cmd/define/` is clean — the reader goroutine is this
      repo's only concurrency.
- [x] Driven end to end in tests through the existing fakes, not by hand.
- [x] `TestRunNoArgsIsUsageError` is rewritten (not deleted) to assert the new
      no-args contract, so that branch is never untested.

## Estimate

*Produced via `brain/data/life/42shots/velocity/estimate-logic-v3.1.md` against `baseline-v3.1.md`. Method A only.*

```estimate
model: estimate-logic-v3.1
familiarity: 1.0
item: issue-spec              design=0.3  impl=0.04
item: smaller-go-module       design=0.06 impl=0.12
item: milestone-review        design=0.0  impl=0.12
item: atlas-docs              design=0.05 impl=0.06
design-buffer: 0.15
total: 0.81
```

Derivation notes:

- **smaller-go-module**, not greenfield: the REPL is a loop around `defineOnce`
  plus a decorator on an existing seam, in a codebase whose fakes and rendering
  already exist. Design takes the ×0.2 spec-quality discount (0.3 → 0.06) — the
  plan pre-resolves the command table, the cache seam, and the TTY rule.
- **impl 0.12** is the scaled top of the 0.2–0.5 band: the concurrency (a read
  that must not swallow Ctrl-C) is the one part with real failure modes.
- **One `milestone-review`**: single-pass work, one boundary, one close.
- `familiarity: 1.0` — same package, same fakes, same author as #1.

Σdesign 0.41 × 1.15 = 0.4715; Σimpl 0.34 × 1.0 = 0.34; total 0.8115 ≈ **0.81**.

## Plan

See `workshop/plans/000002-repl-plan.md`.

- [x] Extract `defineOnce` from `run` (pure refactor, existing tests unchanged).
- [x] `parseREPLLine` — the command table, pure.
- [x] `cachingAudioSource` — decorator, so the fake CDN proves replay is free.
- [x] The loop — reader goroutine + `select` on `ctx.Done()`.
- [x] Wire to bare invocation (TTY on **stdin**), README + atlas.

## Log

### 2026-08-20
- 2026-08-20: closed — Verified through a real pty. A bare return replays the pronunciation and writes NOTHING to stdout — no definition, no announcement (speak is silent by construction; defineOnce owns the "♫ playing N×" line). Confirmed on a real terminal: the second and third presses emit only the Enter echo and the next prompt. Typing a word defines and speaks it; a real SIGINT mid-playback exits 0; zero temp dirs left on every path (normal, piped, interrupted), re-measured from a clean state. echo sycophantic | define now defines the word — new capability, it exited 2 with usage before this issue, which the plan gate correctly flagged as a false claim in the spec. go test -race clean (the reader goroutine is this repo only concurrency), go vet green, GOOS=linux CGO_ENABLED=0 green. Replay costs no network, asserted by fakeCDN.Requested() length 1 across two definitions — timing cannot prove it, playback dominates by two orders of magnitude.; review verdict: FIX-THEN-SHIP

Created as part of the `define-learn` project.

**Done.** Verified through a real pty, which is the only way to test the parts
that matter: prompt appears on a TTY and is reprinted after output, a bare return
replays, a real SIGINT mid-playback exits 0, and zero temp dirs are left behind
on any path (normal, piped, or interrupted).

The plan gate was right about `echo word | define`: I had written that it "still"
works, and it never has — no-args exited 2 with usage. Reading stdin is new
capability here. Correcting that simplified the design: the loop reads stdin
unconditionally and only the prompt is TTY-conditional, so there is no
interactive/batch branch and the whole loop is drivable from a string.

`run` gained `ctx` and `stdin` parameters — the only signature change, both
forced (it previously manufactured `context.Background()` inline, which no test
could cancel). `TestRunNoArgsIsUsageError` was rewritten rather than deleted, so
the no-args branch never went untested while its contract moved.

Timing does not prove the audio cache — playback dominates by two orders of
magnitude — so the proof is `fakeCDN.Requested()` having length 1 across two
definitions.

**Close review — FIX-THEN-SHIP (4 Important), all fixed.**

- **I-1 was a regression I introduced.** Adding `signal.NotifyContext` meant
  Ctrl-C during playback printed `define: afplay: signal: killed` and exited 0 —
  killing `afplay` is *how* cancellation is implemented, so reporting it as an
  error tells the user their own keypress went wrong. Both report sites now
  suppress a diagnostic when `ctx.Err() != nil`. Confirmed on a real pty before
  and after. The review noted this was exactly the residue the plan gate had
  flagged and I had deferred to a manual check that never collected it.
- **I-2:** the >64 KB guard shipped with no test; the whole `exit 1` branch was
  dead to the suite. Pinned, plus a just-under-the-cap case so the guard is not
  over-eager.
- **I-3:** `ErrNoAudio` is permanent but was treated as retryable, so replaying a
  word with no recording re-issued all four candidate URLs every time. Now cached;
  `ErrFetchFailed` stays retryable.
- **I-4:** `-h` still documented only the one-shot form.

**Operator refinement (2026-08-20):** a replay must not advance a line either —
flash `♫ playing N×` once to show the program responded, then erase it and step
back onto the prompt. Implemented with the one piece of cursor control in the
tool, gated on interactive; piped output emits no escape sequence, asserted.

**Operator refinement 2 (2026-08-20):** the indicator is ephemeral *everywhere*,
not only on replay — shown while the sound plays, erased when it finishes. It is
a response acknowledgement, not a record, so the settled screen shows the
definition alone. `options.tty` gates it (a third, distinct terminal question:
`color` and `tty` share a probe but not a meaning, and `stdinIsTerminal` is a
different stream).
