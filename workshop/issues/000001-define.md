---
id: 000001
status: working
deps: []
github_issue:
created: 2026-08-20
updated: 2026-08-20
estimate_hours: 2.75
started: 2026-08-20T09:48:40-07:00
---

# define — NOAD word lookup with Google-style IPA and spoken pronunciation

## Problem

Looking up a word today means opening a browser, typing "define X", and reading
Google's dictionary panel. That panel is worth reproducing in the terminal: it
carries the New Oxford American Dictionary (NOAD) entry, NOAD's distinctive
pronunciation notation (`/ˌsikəˈfan(t)ik/` — `i` for /ɪ/, `a` for /æ/, `(t)`
for the optional flap), and a recorded Oxford pronunciation.

Both halves are reachable without scraping and without an API key:

- **NOAD ships with macOS.** `New Oxford American Dictionary.dictionary` is on
  disk, and `DCSCopyTextDefinition` (public, in the CoreServices SDK header
  `DictionaryServices.h`) returns its entry as flat text. Verified: it returns
  `ˌsikəˈfan(t)ik` for *sycophantic* — character-for-character what Google shows.
- **The audio is a static MP3** on Google's CDN at a URL derivable from the word
  alone. Verified: `.../pronunciation/2022-03-02/audio/sy/sycophantic_en_us_1.mp3`
  → HTTP 200, MPEG layer III.

Scraping the SERP is not an option and was ruled out by measurement: a
`define sycophantic` search returns a 91 KB JS-shell (`enablejs`) containing
zero occurrences of the IPA and no `gstatic.com/dictionary` reference.

## Spec

`define <word>` prints a formatted NOAD entry and plays the pronunciation three
times.

```
$ define sycophantic
sycophantic  syc·o·phan·tic
/ˌsikəˈfan(t)ik/

  adjective
    behaving or done in an obsequious way in order to gain advantage
      "a sycophantic interview"

  DERIVATIVES
    sycophantically /ˌsikəˈfan(t)ək(ə)lē, -ˈfantik(ə)lē/  adverb

  ♫ playing 3×
```

Decisions taken up front (operator, 2026-08-20):

- **NOAD only.** No online fallback. A miss (`rizz`, `unalive` — genuinely
  absent from NOAD) prints a clean diagnostic to stderr and exits non-zero. A
  fallback source is a separate issue if it is ever wanted; note the gaps
  correlate, since both NOAD and the audio CDN trace back to Oxford.
- **Private `github.com/xianxu/tools`** is the home repo.

Parsing is best-effort over a flat string with no schema, so the parser is
built around a **no-data-loss invariant**: every **letter and digit** of the NOAD
entry must survive, in order, into the rendered output. (Punctuation is
deliberately excluded — the renderer restructures it, turning ":" into a line
break and quoting examples. An earlier draft of this Spec said "every
non-whitespace character"; that stronger contract fails on all fixtures at the
header's first "|" and was never what the code asserted.) Structure that is recognized
gets formatted; structure that is not recognized degrades to a plain paragraph
rather than being dropped. This is what keeps a best-effort parser honest
against entries nobody sampled.

## Done when

- [x] `define sycophantic` prints the entry with `/ˌsikəˈfan(t)ik/` and plays audio 3×.
- [x] `define rizz` prints a clean "no NOAD entry" diagnostic on stderr, exit code 1.
- [x] The no-data-loss invariant holds over a fixture corpus that includes the
      known-awkward entries: `bank` (homograph, `bank 1 | baNGk |`, no
      syllabification) and `record` (`record rec·ordnoun | ˈrekərd |` — the
      part-of-speech glues onto the syllabification ahead of the IPA).
- [x] `--no-audio` suppresses playback; `--times N` overrides the repeat count.
- [x] Playback count is asserted by a test, not by ear.
- [x] `go build ./...` and `go vet ./...` are green on non-darwin (stub path).

## Estimate

*Produced via `brain/data/life/42shots/velocity/estimate-logic-v3.1.md` against `baseline-v3.1.md`. Method A only.*

```estimate
model: estimate-logic-v3.1
familiarity: 1.0
item: issue-spec              design=1.0  impl=0.08
item: greenfield-go-module    design=0.3  impl=0.32
item: smaller-go-module       design=0.1  impl=0.2
item: real-api-discovery      design=0.0  impl=0.12
item: milestone-review        design=0.0  impl=0.12
item: milestone-review        design=0.0  impl=0.12
item: atlas-docs              design=0.1  impl=0.06
design-buffer: 0.15
total: 2.75
```

Derivation notes:

- **issue-spec** carries full design (1.0 of the 0.5–1.5 band) and takes **no**
  spec-quality discount — it *is* the plan authoring, including one
  plan-quality gate round that reshaped `Block` and added the pipe rule.
- **greenfield-go-module** (parser + renderer + CLI) is the one genuinely novel
  concern. Design takes the ×0.2 spec-quality discount (1.5 → 0.3) because the
  plan pre-resolves the schema: Rule A's header table, Rule B's verdict table,
  and the entity shapes. Impl sits at the **top** of the 0.3–0.8 band before
  scaling — a schema-less parser guarded by an invariant is the risk in this
  issue.
- **smaller-go-module** (audio: candidates + fetch + playN) is mirror-shaped and
  fully specced, so it takes the low end.
- **real-api-discovery** is one item, not two, and near the floor: both external
  surfaces were already probed before planning (cgo header, CDN path survey), so
  the discovery budget is mostly spent.
- **milestone-review** twice — M1 and close are two real boundaries, each with
  its own fresh-context review.
- **design-buffer 0.15** (not 0.30) per v3.1 step 4: the issue has a thorough
  plan doc.
- All `impl=` values are already scaled to 40% of the v2/v2.1 table per v3.1;
  `familiarity: 1.0` — Go and this SDLC are well-trodden, and the cgo unknown
  was retired by the spike.

Σdesign 1.5 × 1.15 = 1.725; Σimpl 1.02 × 1.0 = 1.02; total 2.745 ≈ **2.75**.

## Plan

See `workshop/plans/000001-define-plan.md`.

- [x] M1 — Definition pipeline: dictionary seam, parser, renderer, `define <word>` prints.
- [x] M2 — Pronunciation: URL derivation, CDN fetch, playback 3×, CLI flags.

## Log

### 2026-08-20

Repo `tools` seeded as an ariadne derivative (`weave link ../ariadne` →
`weave compile`, 99 actions). Issue claimed.

Feasibility settled before planning, so the plan rests on measurement rather
than assumption:

- cgo → `DCSCopyTextDefinition` builds against the **public** SDK header
  (`CoreServices.framework/Frameworks/DictionaryServices.framework/Headers/
  DictionaryServices.h`); an initial hand-written `extern` redeclaration failed
  to compile against it, which is how the header was confirmed public. Only two
  functions are exported — `DCSGetTermRangeInString` and
  `DCSCopyTextDefinition` — so structured markup (`DCSCopyDefinitionMarkup`) is
  private and out of bounds. **We parse flat text.**
- CDN path survey across 10 words: `pronunciation/2022-03-02/…` strictly beats
  the legacy `sounds/oxford/…` and `sounds/20200429/…` paths
  (`gaslighting` 200 vs 404/404; `defenestrate` needs `_2` on the 20200429
  path). Hence an ordered candidate list, not one URL.
- `DCSCopyTextDefinition` returns nothing under the tool sandbox — it needs real
  access to `/System/Library/AssetsV2`. Relevant to how tests are run here.

**Blocked on operator:** `git push` is rejected — the stored GitHub OAuth token
lacks `workflow` scope, and weave delivers a tracked
`.github/workflows/merge-check.yml`. Local commits succeed; `sdlc claim` flipped
status but could not publish. Fix is `gh auth refresh -s workflow`.

Plan-quality gate cleared in 2 rounds (PQ-1..PQ-6 all disposed `addressed`).
Four of the six findings were re-verified against the live API before acting;
PQ-2's *suggested* rule ("IPA iff no ASCII letters") was rejected because
`ˈrekərd` and `baNGk` are mostly ASCII letters — replaced with a word-shape
rule measured across six entries.

Estimate-quality returned **INFO** (non-blocking). Two observations accepted but
deliberately **not** folded into the number:

1. The dictionary seam (Tasks 1–3) has no line item — a real structural gap.
2. `smaller-go-module`'s note says "low end" while `impl=0.2` is the scaled
   *top* of its band. The note is back-fitted; the number stands.

Not re-derived, because the same judge measured `issue-spec design=1.0` as ~3×
high against timestamped wall clock (~20 min from claim to estimate). Adding
~+0.15 for the missing item while leaving a ~−0.7 error in place would move the
total further from reality — estimate theater. The forecast stays as derived and
gets scored honestly at close, where `sdlc actual` measures rather than guesses.
Both findings reach the close review via the gate ledger.

**M1 complete.** The no-data-loss invariant earned its place immediately: it
failed twice on `record`, both times catching **reordering** rather than loss
(rune counts matched exactly — 2704 both sides).

1. The glued `noun` from `rec·ordnoun` was rendered below the IPA, though NOAD
   prints it in the head. Fixed by `Entry.HeadPOS` + `Block.FromHead`.
2. `verb [with object] | rəˈkôrd |` — the grammar label was folded into the
   sense text, moving it after the IPA. Fixed by `Block.Label`, rendered
   between the POS and its pronunciation.

Neither would have been caught by a test asserting "the output contains the
definition", and neither is visible without looking for it. `go build`/`vet`
green, `GOOS=linux CGO_ENABLED=0` green via the stub, live conformance passes
on all 9 fixtures.

**M1 round-4 review + M2 complete.**

Round 4 found two Criticals and, notably, a factual error running through every
artifact: `DCSCopyTextDefinition` is passed a NULL `DCSDictionaryRef`, which means
*search every active dictionary*, not NOAD. The SDK exports no constructor for a
`DCSDictionaryRef`, so the NULL is forced — this is inherent, not a bug to fix.
Consequences: `iPhone`/`iPad`/`MacBook` (three committed fixtures) are Apple
Dictionary entries, which is why they have no pronunciation; 530 of 71,427
reachable entries return Han script. The code comment, `atlas/define.md`, the
README and this Spec all claimed NOAD-only. Corrected in all four.

Also round 4: `opensBlock` rejected `]`, so a part of speech opening straight
after an editorial note (`…The Compleat Angler] verb`) never opened a block —
`complete` and `pulp` had their verb blocks swallowed into a quoted example
(22 entries). One token fixed it.

And the oracle lesson repeated one level up: `strayStress` was honest but
*narrow* — example-separator pipes carry no stress mark, so it could not see
them, and the atlas published "0%" while 2.0% of entries still rendered raw `|`.
Added a second oracle that consults nothing at all (`IndexByte(out, '|')`), and
section text now splits on the separator instead of rendering as one paragraph.

**M2 shipped.** `AudioCandidates` (pure, ordered by the measured survey),
`httpAudioSource` + `fakeCDN` (records request order, proving the walk stops at
the first hit), `Player` + `fakePlayer` + `playN` (repeat loop in the shell so
the count is assertable). Flags `--no-audio`, `--times N`, `--locale`.
`--no-audio` makes **zero** CDN requests, asserted.

Measured end to end: `define sycophantic` 6.56s (3 plays), `--times 1` 1.97s,
`--no-audio` 0.05s. `make install` symlinks into `~/.local/bin`.

**Boundary note:** M1 never received a SHIP verdict across four review rounds;
each round found real, live-verified defects. M2 was built while round 4 was in
flight, at the operator's direction, so M1's boundary folds into the issue close
— one review over the full window rather than a milestone review of a window that
already contains M2. Recorded rather than papered over.
