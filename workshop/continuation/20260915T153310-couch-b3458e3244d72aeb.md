---
type: continuation
slug: couch-b3458e3244d72aeb
agent: codex
created: 2026-09-15T15:33:10
branch: 000066-define-language-regions-and-bilingual-layout
worktree: /Users/xianxu/workspace/tools
issues: [000062, 000065, 000066]
---

# Continuation: couch-b3458e3244d72aeb

## NEXT ACTION

Run `sdlc state`, then start implementation on branch
`000066-define-language-regions-and-bilingual-layout`. Change-code passed:
plan quality accepted round 2 (PQ-1 addressed), estimate-quality INFO only.
Estimate is 7.117 full-issue ship hours, not remaining time. Implement
#66 from its approved durable plan, starting with failing native Oxford structure
and full-background-cell tests. Settle the shared structured-output contract
before delegating parser and painter work. **Operator already approved the final
preview (“yes, looks great”); do not ask for layout approval again.**

## State of play

#62 and #65 are done and merged (PRs 47 and 48). #66 corrects #65's visual result:
ink-only backgrounds looked patchy, and Oxford was flattened. #64 adaptive
learner behavior remains untouched. No runtime code/tests changed for #66 yet.
Claim/start-plan are complete; implementation gate is complete.
This continuation follows context pressure under AGENTS §14; the writer owns
restart. Work should resume autonomously through implementation, verification,
SDLC close review, PR and merge.

Repo/worktree: `/Users/xianxu/workspace/tools`. Current branch is
`000066-define-language-regions-and-bilingual-layout`. Preserve the unrelated untracked
`workshop/issues/000048-play-a-sitting-without-leaving-the-loop.md`; never stage
or remove it. Its missing frontmatter warning is unrelated.

## Thread arc & user model

The user wants bilingual output to be instantly readable in both dark and light
terminals. Initial language-span tint worked technically but produced text strips;
the screenshot exposed this and Oxford's lost structure. The first whole-row
preview still alternated backgrounds inside a dictionary, which the user rejected
as distracting zebra striping. The final approved design uses **one background per
entire dictionary section**, with bilingual example/translation pairs together on
one logical row and natural wrapping. Their priority is coherent visual grouping
and preserved dictionary typography, not token-by-token language coloring.

Approval is explicit and current. No user questions remain. Avoid additional
ceremony or revisiting resolved design choices.

## Artifact map

Read `workshop/issues/000066-define-language-regions-and-bilingual-layout.md`, then
`workshop/plans/000066-define-language-regions-and-bilingual-layout-plan.md`.
They contain the final contract, consumer sweep, streaming transitions, direct
function-level test strategies, commands and visual acceptance. Revisions explain
superseded drafts. The plan-gate sibling file records PQ-1 and its disposition;
this finding only requested explicit named function/input/oracle mapping, now
present. Estimate sources and arithmetic are in the issue's Estimate section.

`/tmp/define66-region-preview.html` is the approved dark/light + es/en illustration,
not production proof. `/tmp/define66-rendir-native.json` is an exact installed
Oxford capture (one selected record, HTML 11,404 bytes, Text 1,960 bytes, ID
`s_b-es-en0040662`); `/tmp/define66-rendir-native-tree.txt` is its structure digest.
Promote the capture with provenance/regeneration instructions for tests. Actual
output must still be captured and inspected visually before claiming success.

`definitions.go` currently discards Oxford structure into Text plus ownership,
then renderDefinitions reparses it with NOAD ParseEntry. `bilingual.go`/related
ownership helpers already validate native XML identity/size/depth: consolidate the
walk instead of adding an independent class grammar. Native structure is gramb
A/B/C; numbered/nested lettered semb; exg paired ex/trans; idiom wrappers and
inline emphasis. Preserve ordered leaves exactly once and use a neutral readable
fallback for malformed/unverified records.

`language_style.go` implements old text-run tint. `screen.go` owns current width,
physical-row clipping/paint and footer path. `selection_frame.go` keeps selectable
cells; never put synthetic paint padding in styled source text or clipboard.
`main.go` writeRendered/regionWriter is the atomic output seam to extend.
`practice_language.go`, import-free `play.Presentation`, and boardFooter must carry
metadata; full definition reveals retain dictionary-section roles.
`answer_language.go` and `answerwrap.go` own streaming display. Reuse the #65
annotation/control decoder unchanged and preserve clean logical history.

## Decisions & dead ends

With `/lang es`, tint all of verified Spanish primary and keep all Oxford English-
explanation supplement neutral, even its Spanish quotations. A shown English-
explanation section under `/lang en` is wholly tinted. Source provenance and
presentation role are separate. Unknown all-active-dictionary fallback stays
neutral; never substitute current study language for dictionary provenance.

Paint at current physical width, including headings/indentation/trailing cells/
interior blank rows. Keep foreground/emphasis, answer exclusions and selection.
Metadata follows unpadded text through wrapping, clipping, screen and footer;
pipes/off/no-color stay clean. Historical language is frozen per output; old rows
clip on resize rather than reflow. Exit transcript/one-shot TTY share painting.

Outside dictionary blocks, physical mixed/unknown rows are neutral. Buffer one
unfinished physical model row for BOTH live and append-only colored sinks; no
provisional tint or retroactive repaint. Finalize on wrap/newline/finish and keep
completed ownership immutable. Preserve existing 64 KiB pending cap and history
on display failure. Resize only relays pending text at new width. The plan's
explicit transition table and chunk/sink/cancel tests prevent paragraph-vs-row
ownership ambiguity.

## Lessons learned

Background tests must assert blank terminal cells via an independent state oracle;
ANSI substring checks allowed the original poor result. Preview approval alone
is not production verification. Preserve actual native/SSE fixtures, don't invent
external-service outputs. Full Go tests here can take ~130 seconds; boundary
reviews can take several minutes. Keep user updates within 60 seconds.

AGENTS authorizes bounded subagents after shared interfaces settle. Prior agents
`/root/tint_dictionary` and `/root/tint_practice` investigated Oxford/screen and
may be reusable; `/root/language_styling_spec` knows the model path. No agents have
implemented #66. SDLC alone dispatches fresh close review; do not duplicate it.
Do not guess actual hours. Keep path-scoped commits and a GPT-6 Co-Authored-By
trailer. `sdlc pr` then `sdlc merge --yes` is the authorized publish path.
