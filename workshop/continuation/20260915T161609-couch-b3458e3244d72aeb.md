---
type: continuation
slug: couch-b3458e3244d72aeb
agent: codex
created: 2026-09-15T16:16:09
supersedes: couch-b3458e3244d72aeb
branch: 000066-define-language-regions-and-bilingual-layout
worktree: /Users/xianxu/workspace/tools
issues: [000062, 000065, 000066]
---

# Continuation: couch-b3458e3244d72aeb

## NEXT ACTION

Inspect the uncommitted BR3 fix and new tests, then commit it and run the full
postcommit suite (`go test ./... -count=1`, about 130s), focused race and strict
native/PTY checks. Focused integrated tests and agent core/screen race already
pass; diff check is clean. Re-run `sdlc close --issue 66 --verified '<evidence>'`
then follow its verdict through PR and `sdlc merge --yes`. User has authorized
implementation through merge and approved the final visual design; do not ask
again. Preserve this existing Pair thread and prompt history. Writer owns restart.

## State of play

Repo `/Users/xianxu/workspace/tools`, branch
`000066-define-language-regions-and-bilingual-layout`. #62/#65 merged, #64 untouched.
#66 remains working: implementation is committed in `77a5cfe`; first review fixes
in `a365ee5`. Last full postcommit suite passed (128.902s), strict real native/PT Y
checks passed, actual terminal screenshots inspected. Two close reviews returned
REWORK. Round 1 BR1 finalized-row paint erased by terminating plain newline and
BR2 inaccurate plan declaration table are fixed, committed, and accepted in
round 2. Round 2 BR3 found tinted terminal serialization losing overlong-word
suffixes. That fix is now implemented and focused-tested but UNCOMMITTED.

Preserve unrelated untracked issue
`workshop/issues/000048-play-a-sitting-without-leaving-the-loop.md`; never stage or
remove it. All agents are done, no active tool processes. Root owns all commits.
No PR yet. `sdlc state` and issue are workflow truth. Actual is measured, never
hand-enter it. First review measured 2.35h, second 2.78h; estimate 7.117 full issue.

## Thread arc & user model

User wants coherent bilingual dictionary sections with full physical-cell
background, not text strips or alternating language stripes. Approved final
preview explicitly (“yes, looks great”). Preserve Oxford hierarchy and paired
Spanish example/English translation. Current question was whether TUI used a
React-style library; answered existing custom Go/ANSI liveScreen (screen buffers,
redraw, resize, selection). It is a question during active work, not cancellation
or request to migrate UI frameworks. No questions need resolving before work.

## Artifact map

Read issue `workshop/issues/000066-define-language-regions-and-bilingual-layout.md`
and durable plan `workshop/plans/000066-define-language-regions-and-bilingual-layout-plan.md`.
Their Log/Revisions record all verification, contracts, revisions and latest BR3.
Sibling `-close-review.md` and `-close-gate.md` record both reviews and dispositions;
do not manually rewrite ledger status. Plan's final commit/close/publish box is
still unchecked; do not claim finished before acceptance. Atlas and define README
already updated. `/tmp/define66-pr-body.md` is draft PR body for use after sdlc pr.

Uncommitted BR3 files:
- `cmd/define/output_layout.go`: `outputWrappedRows` and `outputPhysicalRows`
  split residual overlong words into whole display units via walkSelectionRows.
  layoutOutput projects original cells through wordwrap then hard split, preserving
  clicks/exclusions. Oversized single glyph (width1 vs glyph2) stays intact neutral.
  Oversized input fallback preserves text but drops unsafe click metadata.
- `definitions.go`: renderDefinitionOutput returns layoutOutput at opt.Width,
  ensuring initial unpinned-screen ingress is physical before historical clipping.
- `practice_output.go`: uses outputWrappedRows and projectDisplayText so pure
  target first row in a hard-wrapped mixed logical row retains tint.
- `dictionary_language.go`: shared projectLanguageText with strict native wrapper
  projectDictionaryText(false), display-only projectDisplayText(true) permits
  generated wrap whitespace while preserving sequential exact glyph matching.
- New `physical_output_test.go`: dictionary source conservation/history does not
  reflow; practice physical-row ownership.
- New `terminal_serialization_test.go`: dictionary headword/body dark/light/off
  source conservation, wide+combining units, cell backgrounds, click/exclusions,
  whitespace offsets, blank/trailing rows, width0, exceptional width1 glyph.
- issue Log, plan revision, generated review/gate artifacts.

Focused integrated batch passed:
`go test ./cmd/define -run '^Test(DefinitionOutput|PracticeLongRun|TerminalSerialization|OutputLayout|DictionaryProjection|Practice)' -count=1`
log `/tmp/define66-long-layout.log` (1.700s). Agent row_painter reports core/screen
race tests pass. Full suite/strict PTY not yet rerun after BR3. Add meaningful
lesson on preserving source through terminal hard wrapping, inspect final diff,
commit exact #66 files (exclude #48), then full suite after commit catches plan
declaration guard regressions that precommit suite cannot see.

Existing verification logs: `/tmp/define66-postcommit.log` (full green before BR3),
`/tmp/define66-review-race.log`, `/tmp/define66-postreview-pty.log`,
`/tmp/define66-close2.log` (latest review). Native/PT Y command:
`CONFORMANCE_STRICT=1 go test -tags conformance ./cmd/define -run '^Test(BilingualNativeRendirLayout|PTYLanguageTint|PTYNativeRendirSectionLayout)$' -count=1`.
Earlier go vet, Linux build, Oxford+decoder fuzz and function mutations passed;
footer metadata mutant initially survived, added real cell consumer test that
kills it. Actual screenshot `/tmp/define66-visual.png` inspected; generator
`/tmp/define66-visualize.py`, actual captures `/tmp/define66-actual-*` and
`/tmp/define66-pty/*.raw`. Real screenshot shows correct dark/light coherent
Spanish primary and neutral structured Oxford supplement.

## Decisions & dead ends

Use existing renderer, not a framework migration. Metadata `renderedOutput`
(text, regions, rows) is unpadded and frozen; painter fills only terminal cells.
`rowPaint` owns background plus display-column exclusions. Transcript stays source;
PaintedTranscript is terminal exit rendering; OutputTranscript transfers metadata
for nested sitting. Plain/structured newline/CRLF preserves finalized partial-row
paint; substantive append invalidates it. That was BR1.

One entire dictionary section has one presentation background independent of
source ownership: /lang es Spanish primary tinted, Oxford supplement neutral
including Spanish examples; /lang en shown Oxford entirely tinted. Unknown
fallback neutral. Native XML parser preserves A/B/C/number/letter hierarchy,
idioms/emphasis and ordered leaves; bad provenance falls back readable+neutral.
Native fixture `testdata/bilingual/rendir.json` is installed capture with provenance.

Streaming decoder unchanged, new ownedAnswerWrapWriter buffers unfinished physical
rows for both sinks, mixed/unknown prose neutral, cap64KiB, clean cancellation and
history. Legacy answerWrapWriter stays unchanged. Width0 is plain contract.
History clips on resize and never reflows. New initial hard wrapping must not
change this historical rule. Do not solve BR3 by disabling history clipping.
Practice key decoration is explicit and answer exclusions retain semantic colors.

## Lessons learned

SDLC close itself runs mandatory fresh review; do not add duplicate review.
Reviews take several minutes and may run tests: keep user updated <=60s, do not
mutate HEAD or issue during review. REWORK needs fix+commit+close rerun.
FIX-THEN-SHIP permits bookkeeping after fixes as gate instructs. Commit conventions
include issue and Co-Authored-By: GPT-6 <noreply@openai.com>.

Test the full caller termination sequence, not only a streaming writer in
isolation; run full suite after commit for plan diff guards. Test physical source
conservation separately from background cells, since a visually filled line can
still have lost its suffix. Strict source validation and display whitespace
projection need separate entry points sharing one matching implementation.
