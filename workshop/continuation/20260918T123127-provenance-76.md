---
type: continuation
slug: provenance-76
agent: claude
created: 2026-09-18T12:31:27
branch: main
worktree: /Users/xianxu/workspace/tools
issues: [000076]
---

# Continuation: provenance-76

## NEXT ACTION

1. Fold in plan-gate advisory **PQ-4** (Minor, `unbacked-existing-behavior-claim`, 2nd in its family):
   - In `workshop/plans/000076-orphaned-source-provenance-plan.md`, the M-join mutation survives the "changed word" (`"red red"`→`"red green"`) and "joined words" cases. Those are caught by the trailing-leftover guard, not the glyph branch. Only a **length-preserving** substitution (`"red red"`→`"red bed"`) kills M-join.
   - Add that case to Task 1's strategy line. Change the M-join expectation in Task 3 and the Done-when row to name it.
   - Apply the family rule: any predicted test/guard outcome in the plan carries a "measured <date>" beside it, or is deleted.
   - Record it under the plan's `## Revisions` (last section).
2. Derive the estimate. Run `sdlc estimate-source`, then add `estimate_hours:` to the issue frontmatter and a fenced ```estimate block under `## Estimate` whose design/impl hours sum to it.
3. Re-run `sdlc change-code --issue 76`. Plan-quality already cleared (round 2, no open blocking findings), and the gate file remembers. The estimate gate is the only thing it refused on. It then creates the branch (in-place by default).
4. Execute the plan's Tasks 1→4. **The plan file lands in the Task 1 commit, not before**: on its own it reddens `TestPlanCitesTestsThatExist`, whose three cited tests Task 1 writes.

## State of play

- **#76** (`workshop/issues/000076-orphaned-source-provenance.md`) is `working`. It was claimed and pushed to origin/main.
- The Spec (decision: delete), Plan and Log are committed locally on `main` as `c55758d` (`sdlc issue sync`), **not pushed**. `change-code` pushes when it syncs.
- The durable plan and the gate file are **untracked** on disk under `workshop/plans/`. Keep them uncommitted until Task 1 (see NEXT ACTION 4):
  - `workshop/plans/000076-orphaned-source-provenance-plan.md`
  - `workshop/plans/000076-orphaned-source-provenance-plan-gate.md`
- No code has changed yet. There is no branch yet: `change-code` creates it.
- **Unrelated working-tree changes are NOT mine.** They appeared while the operator was editing AGENTS.md, so leave them alone and never stage them with #76 work:
  - `.github/workflows/merge-check.yml` (modified)
  - `Makefile` (type change)
  - `bootstrap.sh` (modified)
  - `scripts/issue-sync.sh` (deleted)
  - `scripts/merge-checks.d/40-duplicate-issue-id.sh` (untracked)
- **Baseline on `main`:** everything is green. Three pty tests fail only under the Bash sandbox ("operation not permitted") and pass outside it: `TestLanguagePromptStartup`, `TestLanguageTintInvocation` and `TestSavedSchemeGovernsALookup`. The full suite takes about 135s.

## Thread arc & user model

- The operator said "work on #76", and mid-planning: "generally, I'd like to delete unused features". This is saved as auto-memory `delete-unused-features`. Default to deleting whole orphan chains; don't hunt for a consumer.
- The operator approved the plan ("continue"). Then they stopped the session to reload AGENTS.md changes.
- The CLAUDE.md now in force adds two things: the quick-flow shell decides whether a durable plan is needed, and `sdlc issue sync` checkpoints the design (#206).

## Artifact map

- The plan holds the full inventory. It covers the Core-concepts tables (the deleted, modified and new rows, plus a Tests table) and Tasks 1–4 with the shared `CHECK` after each commit. It also holds the Done-when table, and a `## Revisions` entry for gate round 1.
- The gate ledger is `…-plan-gate.md`: PQ-1, PQ-2 and PQ-3 are addressed, and PQ-4 is advisory.
- The code the chain touches is in `cmd/define/`: `parse.go` (restored wholesale from `62c6a66^`), `definitions.go`, `render.go`, `bilingual_layout.go`, `dictionary_language.go` (deleted; `projectDisplayText` moves to `language_text.go`), `atlas/define.md:638` and the `parseBilingualDocument` paragraph around line 2376.

## Decisions & dead ends

- **Task order is tests → writers/readers → declarations,** so that every commit builds under both tag sets and passes the full suite. This came from PQ-1: the first order left Task 2 uncompilable, because `definitions.go` still wrote `entry.source`.
- **Don't predict red sets.** PQ-2: `currentTruthOnly` strips names on `| … | deleted |` rows, so `deleted` rows are never checked. My earlier claim that they were "red-first pins" was wrong; the Log records the correction.
- `projectDisplayText`'s plan row is `new` at `language_text.go`, not `modified`. A `modified` row there would have been false until Task 3.
- `parse.go` restore: `diff 62c6a66^` shows only offset plumbing, so `git checkout 62c6a66^ -- cmd/define/parse.go` is the entire parser deletion. The check is an empty diff.

## Lessons learned

- A synced issue lands on `main`. It must not backtick-cite a `Test*` that doesn't exist yet: `TestPlanCitesTestsThatExist` reads issues, with no in-progress exemption.
- Any claim about what a guard or mutation *will* do needs a measurement beside it. That family has two findings now: PQ-2 and PQ-4.
