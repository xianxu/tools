# Build artifacts. `make build` writes bin/<name>; `go build ./...` drops the
# binary at the repo root, which is how bin/define and ./define first got
# committed. Both paths are ignored so neither recurs.
bin/
/define
.DS_Store

/AGENTS.md
/CLAUDE.md
/GEMINI.md
/.claude/skills/
/.agents/skills/
/.claude/settings.json
/.colima/
/construct/scripts/vm-log.sh
/construct/generated/

# define writes its deck to the CURRENT directory, and this repo's developers run
# it from the checkout — a failed lookup in the repo root created an untracked
# events/ during review.
/words/
/events/

# `go build ./` inside a cmd/<name>/ directory writes an extensionless binary
# named after that directory — a 9.6 MB one was committed this way. bin/ is the
# sanctioned output. The pattern below covers the known tools; the general case
# is enforced by TestNoCommittedBinaries, which fails for any future cmd/ too.
/cmd/*/define
